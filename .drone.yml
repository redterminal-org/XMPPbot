---
kind: pipeline
type: docker
name: pytest

steps:
- name: build pytest
  image: python:3.10
  commands:
    - pip install -r requirements.txt
    - pip install -r requirements-dev.txt
    - pytest
  trigger:
    branch:
      - main
    event:
      - tag
