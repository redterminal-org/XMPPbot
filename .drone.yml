---
kind: pipeline
type: docker
name: python3.10

steps:
- name: pytest
  image: python:3.10
  commands:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt
  - pytest

  when:
    event:
      - tag
